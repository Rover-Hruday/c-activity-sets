#include<stdio.h>
int input_side()
{
  int side;
  printf("Enter the side of the triangle\n");
  scanf( "%d",&side);
  return side;
}

int check_scalene(int a,int b,int c)
{
  if(a!=b && b!=c && c!=a)
    
}
